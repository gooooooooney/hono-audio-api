# Hono API 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ================================
# OpenAI API 配置（必需）
# ================================
# OpenAI API 密钥
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI API 基础 URL（可选，默认为官方 API）
OPENAI_BASE_URL=https://api.openai.com/v1

# Whisper 模型名称（可选，默认为 whisper-1）
OPENAI_MODEL=whisper-1

# ================================
# 应用配置
# ================================
# 应用端口（可选，默认 3000）
PORT=3000

# 运行环境（可选，默认 production）
NODE_ENV=production

# API 路径前缀（可选，默认 /api/v1）
API_PREFIX=/api/v1

# CORS 允许的源（可选，默认 *）
CORS_ORIGIN=*

# ================================
# 日志配置
# ================================
# 日志级别（可选，默认 info）
# 选项: error, warn, info, debug
LOG_LEVEL=info

# ================================
# VAD（语音活动检测）配置
# ================================
# VAD 置信度阈值（可选，默认 0.4）
# 范围: 0.0 - 1.0，越高越严格
VAD_THRESHOLD=0.4

# 最小语音段持续时间，毫秒（可选，默认 200）
VAD_MIN_DURATION=200

# ================================
# 性能调优配置（可选）
# ================================
# Node.js 最大内存限制（MB）
NODE_OPTIONS=--max-old-space-size=512

# UV 线程池大小（用于异步 I/O）
UV_THREADPOOL_SIZE=4
EOF < /dev/null